# Work Log — 2026-07-22

Pair with the actual ManicTime export for this date to build the invoice per `SAFE_SQ_INVOICE_GUIDELINES.md`. Task descriptions below are ready to use; hours and time blocks need to come from ManicTime.

---

## 1. Stakeholder communication — repo access, LPIS environment question

**Domain:** INT · **Phase:** Integrate · **Activity:** Stakeholder Communication

Drafted and sent a consolidated message to Steven covering two outstanding blockers (repo access to safesq-registry/paperless/fsid-knowledge, and whether LPIS runs on the same Supabase project or a separate environment), plus status notes on the traffic-ticket case-type default and the confirmed manual (non-automated) state of the Matter365 intake gate. Steven redirected repo-access and IT questions to Kevin; drafted and sent a follow-up message to Kevin covering both items.

## 2. Repository setup — Intelligent Intake Phase 1

**Domain:** OPS · **Phase:** Build · **Activity:** Environment Setup

Created a new private GitHub repository (`komal-safesq/safesq-intelligent-intake`) per Steven's authorization to build on a personal-but-firm-linked account, intended to merge into `safesq-registry` later. Scaffolded the Supabase CLI structure, linked to the live project (`ilcztffrkwfphriuvebr`), added the Phase 1 Intelligent Intake design document, committed and pushed.

## 3. Database migration history repair

**Domain:** DB · **Phase:** Integrate · **Activity:** Database Migration

Attempted `supabase db pull`; hit the known migration-history mismatch (189 migrations existing only in the live database, never exported to git). Ran `supabase migration repair --status reverted` across all 189 migration IDs (recovered from a background-process nesting error mid-run, resumed and completed cleanly). Retried the pull; hit a new blocker requiring Docker (not installed locally). Attempted `supabase db dump` as an alternative, also Docker-dependent. Identified a Docker-free path via direct `pg_dump`/`psql` connection using the project's database password; provided setup instructions. Not yet completed, pending the password lookup/reset on Steven's or Komala's end.

## 4. Document review — three items forwarded by Steven

**Domain:** RES · **Phase:** Analyze · **Activity:** Documentation Review

- Reviewed and explained an AI-generated Krisp (meeting-transcription tool) integration proposal; flagged that its proposed `contacts_canonical`/`contact_identifiers` design substantially duplicates the existing `v_fsid_signal_graph` identity resolver, a potential rebuild-what-exists risk before any code gets written.
- Reviewed and explained a privileged discovery roadmap for an active litigation matter (Birnbaum v. QPS Windows & Doors, Index No. 528959/2021).
- Reviewed the WhatsApp thread and draft AA-33 reopening form context for a real client matter (Aviva Landau/Kramer), clarified scope with Steven before proceeding.

## 5. Birnbaum v. QPS — discovery schedule integrated into live calendar/task system

**Domain:** DB · **Phase:** Integrate · **Activity:** Data Integration

Confirmed the matter already exists in `nyscef_case` (id 18, docket 528959/2021). Retrieved and verified real column definitions and enum values for `case_calendar_event`, `matter_task`, and `case_calendar_subscription` (declined to trust an earlier AI-generated schema summary, per the discipline established this session). Wrote and executed SQL to populate: 6 real calendar events (service deadlines, both EBTs, Note of Issue) via a CTE-chained insert, debugged two type-casting errors (enum casts, timestamp casts) across iterations; 8 matter tasks tied to those events and to the roadmap's open items (including the Doc 73 verification flag); 2 calendar subscription rows (Outlook Calendar, Planner) for Steven. Flagged two real data gaps found in the process (`justice_name`, `case_type` both null on the existing matter record). Drafted and sent a completion summary to Steven.

## 6. dmv-aa-etl project — full orientation and Phase 0/1 execution

**Domain:** DB · **Phase:** Validate · **Activity:** Schema Verification / Data Analysis

Reviewed a new project repository Steven built (`dmv-aa-etl`), scoping DMV AA-series form automation from the document ETL pipeline, with a real reference case (the same Landau/Kramer matter). Read all five orientation docs (README, CONTEXT.md, REFERENCE_CASE.md, SCHEMA_FINDINGS.md, TASKS.md, OPEN_QUESTIONS.md).

**Phase 0 — independent schema re-verification (not taking the handoff doc at face value):**
- Pulled full live column lists for `ticket_docket_canonical` and `dim_timekeeper`.
- **Found and corrected a significant error:** the original doc and Steven's own summary claimed `suspension_*`, `stay_*`, `appeals_board_*`, and `art78_*` columns already existed and should not be re-added. None of them exist. Directly relevant since the reference case is a suspension needing appeals/reopening tracking.
- **Resolved Open Question 4:** the "dangling FK" diagnosis for `engagement_id`/`venue_id` was wrong; both have real foreign keys to `traffic_engagement` and `ref.venues`. No schema action needed.
- Found actual row counts had drifted significantly from the documented figures (two tables roughly doubled same-day).
- Rewrote and committed a corrected `SCHEMA_FINDINGS.md`. Sent a summary to Steven flagging the suspension/appeals-columns finding specifically before migration 001 proceeds.

**Phase 1 — reproducible orphan report and full classification:**
- Built and ran a regex-based orphan-detection query against `inbound_email_attachments`, filtered known noise (English words matching the 10-character pattern), confirmed **14 real orphaned tickets**, including both of Aviva Landau's actual tickets, direct empirical confirmation of the reference case.
- Classified all 14 by root cause, going beyond the single explanation in the existing docs:
  - 9 tickets: confirmed companion-ticket promotion gap (Landau/Kramer pair, a 3-ticket Bayla Schulman cluster extending an earlier finding from this session, a Simon Spitzer pair, a named-but-unconfirmed Rivkah Tzivin case).
  - 2 tickets: a distinct, previously unidentified failure mode, a whole-household promotion failure (Israel Landau's separate, unrelated tickets), not explained by the companion-ticket bug.
  - 3 tickets: single tickets with no companion that still failed to promote, contradicting the documented assumption that single-ticket matters work correctly, a second, unnamed promotion defect.
  - 1 ticket: correctly rejected (a parking summons, not a traffic ticket, working as intended).
  - 1 ticket: ambiguous, insufficient information to classify without further lookup.
- Wrote and committed the full findings to `ORPHAN_REPORT.md` (two commits, initial report then full classification).

---

**Repos touched today:** `safesq-intelligent-intake` (created), `dmv-aa-etl` (5 commits: schema corrections, orphan report, two classification updates).
